name: Golden Keyword Miner Cron

on:
  schedule:
    - cron: '*/5 * * * *' # Run every 5 minutes (UTC) - 공격적 모드
  workflow_dispatch: # Allow manual trigger

jobs:
  mine:
    runs-on: ubuntu-latest
    timeout-minutes: 8 # 5분 크론 내에서 루프 호출로 처리량 극대화
    concurrency:
      group: miner-cron
      cancel-in-progress: false
    
    steps:
      - name: Trigger Miner API
        id: mining
        run: |
          set -e  # 에러 시 즉시 중단
          
          # 환경 변수 검증
          if [ -z "${{ secrets.PROD_URL }}" ] || [ -z "${{ secrets.CRON_SECRET }}" ]; then
            echo "❌ ERROR: PROD_URL or CRON_SECRET is not set in GitHub Secrets."
            exit 1
          fi
          
          START_TIME=$(date +%s)
          echo "🚀 Starting mining batch at $(date -u +"%Y-%m-%d %H:%M:%S UTC")"

          # 최대 수집량 최적화:
          # - GitHub schedule minimum is 5 minutes.
          # - Inside each run, repeatedly call the miner API for ~3 minutes to maximize daily throughput.
          # - Keep each API call within Vercel 60s by using controlled concurrency.
          # Goal: 최대 키워드 수집량 달성
          # calls/day ~= (RUN_FOR_SECONDS/INTERVAL_SECONDS) * 288
          RUN_FOR_SECONDS=180  # 3분간 실행 (120 → 180)
          INTERVAL_SECONDS=20  # 20초마다 호출 (30 → 20, 더 자주 호출)
          END_TIME=$((START_TIME + RUN_FOR_SECONDS))

          # 최대 수집량 최적화 파라미터:
          # - expandBatch=100 => ~100 Ad API calls per miner execution (44 → 100)
          # - expandConcurrency=8 => match 8 Ad keys (4 → 8, 동시 처리 증가)
          # - minSearchVolume=1000 => 최소 검색량 1000 이상만 수집
          # Target: (180/20=9 calls/run) * 100 * 288 = 259,200 Ad calls/day
          # 예상 키워드 수집: 259,200 시드 × 20개 키워드/시드 = 5,184,000개/일 (이론상)
          # 실제 안전: 약 150,000-200,000개/일
          QUERY="task=expand&expandBatch=100&expandConcurrency=8&minSearchVolume=1000&maxRunMs=55000"

          TOTAL_OK=0
          TOTAL_FAIL=0

          while [ $(date +%s) -lt $END_TIME ]; do
            echo "📡 Calling miner: ${{ secrets.PROD_URL }}/api/miner/execute?$QUERY"

            # Per-call retry (keep short)
            MAX_RETRIES=2
            RETRY_COUNT=0
            SUCCESS=false

            while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$SUCCESS" = "false" ]; do
              RESPONSE=$(curl -X GET "${{ secrets.PROD_URL }}/api/miner/execute?$QUERY" \
                -H "CRON_SECRET: ${{ secrets.CRON_SECRET }}" \
                -w "\n%{http_code}" \
                --max-time 75 \
                --silent \
                --show-error) || true

              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              BODY=$(echo "$RESPONSE" | sed '$d')

              if [ "$HTTP_CODE" = "200" ]; then
                SUCCESS=true
                TOTAL_OK=$((TOTAL_OK + 1))
                echo "✅ HTTP $HTTP_CODE"
                echo "$BODY" | jq '.' || echo "$BODY"
              else
                RETRY_COUNT=$((RETRY_COUNT + 1))
                echo "⚠️  HTTP $HTTP_CODE (retry $RETRY_COUNT/$MAX_RETRIES)"
                echo "Response: $BODY"
                sleep $((RETRY_COUNT * 3))
              fi
            done

            if [ "$SUCCESS" = "false" ]; then
              TOTAL_FAIL=$((TOTAL_FAIL + 1))
              echo "❌ Call failed after retries"
            fi

            sleep $INTERVAL_SECONDS
          done

          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          echo "📈 Loop summary: OK=$TOTAL_OK FAIL=$TOTAL_FAIL"
          echo "⏱️  Total execution time: ${DURATION} seconds"
          echo "🎉 Mining batch completed successfully!"

