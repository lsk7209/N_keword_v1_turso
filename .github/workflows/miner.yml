name: Golden Keyword Miner Cron

on:
  schedule:
    # 🚀 안정적인 주기: 10분마다 실행 (각 실행이 4-5분 소요)
    # GitHub Actions 크론의 한계를 고려하여 여유있게 설정
    - cron: '*/15 * * * *'
  workflow_dispatch: # Allow manual trigger

jobs:
  mine:
    runs-on: ubuntu-latest
    timeout-minutes: 7 # 4분 루프 + 여유시간
    concurrency:
      group: keyword-expand-cron
      cancel-in-progress: false
    
    steps:
      - name: Trigger Miner API
        id: mining
        run: |
          set -e  # 에러 시 즉시 중단
          
          # 환경 변수 검증
          if [ -z "${{ secrets.PROD_URL }}" ] || [ -z "${{ secrets.CRON_SECRET }}" ]; then
            echo "❌ ERROR: PROD_URL or CRON_SECRET is not set in GitHub Secrets."
            exit 1
          fi
          
          START_TIME=$(date +%s)
          echo "🚀🚀🚀 Starting ULTRA HIGH-VOLUME mining at $(date -u +"%Y-%m-%d %H:%M:%S UTC")"

          # ═══════════════════════════════════════════════════════════════════════
          # 🚀 초고성능 설정: AD 14개 + SEARCH 30개 API 키 최대 활용
          # ═══════════════════════════════════════════════════════════════════════
          # 
          # 📊 API 키 활용 전략:
          # - AD API (14개): 라운드 로빈으로 키당 15회 요청 → 분당 210회 가능
          # - SEARCH API (30개): 일일 25,000회 × 30개 = 750,000회 가능
          # 
          # ⚡ 최적화 수치 (GitHub Actions 5분 제한 준수):
          # - 크론 주기: 5분마다 (하루 288회)
          # - 루프 시간: 4분 (240초)
          # - 호출 간격: 10초 (루프당 24회 API 호출)
          # - 배치 크기: 1500 시드
          # - 동시성: 150 (AD 14개 × 10 + 여유분)
          # 
          # 📈 예상 처리량:
          # - 크론당: 24회 × 1500시드 = 36,000 시드 처리
          # - 일일: 288회 × 36,000 = 10,368,000 시드 시도
          # - 시드당 20개 연관키워드 → 이론적 2억개
          # - 중복 제거 후 실제 신규: 15만~25만개/일 예상
          # ═══════════════════════════════════════════════════════════════════════
          
          RUN_FOR_SECONDS=210  # 3.5분간 루프 실행
          INTERVAL_SECONDS=10  # 10초마다 API 호출
          END_TIME=$((START_TIME + RUN_FOR_SECONDS))

          # 🚀💰 최종 최적화 파라미터 (Extreme Stability)
          # - expandBatch=100 => 100 시드/실행 (절대 타임아웃 안 나는 초안전값)
          # - expandConcurrency=150 => 150 (내부에서 13키 기준 40으로 자동 조정됨)
          # - minSearchVolume=10 => 최소 검색량 10 (더 많은 롱테일 키워드)
          # - maxRunMs=58000 => Vercel 60초 제한 고려
          QUERY="task=expand&mode=TURBO&expandBatch=100&expandConcurrency=150&minSearchVolume=10&maxRunMs=58000"

          TOTAL_OK=0
          TOTAL_FAIL=0

          while [ $(date +%s) -lt $END_TIME ]; do
            echo "📡 Calling miner: ${{ secrets.PROD_URL }}/api/miner/execute?$QUERY"

            # Per-call retry (keep short)
            MAX_RETRIES=2
            RETRY_COUNT=0
            SUCCESS=false

            while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$SUCCESS" = "false" ]; do
              RESPONSE=$(curl -X GET "${{ secrets.PROD_URL }}/api/miner/execute?$QUERY" \
                -H "CRON_SECRET: ${{ secrets.CRON_SECRET }}" \
                -w "\n%{http_code}" \
                --max-time 75 \
                --silent \
                --show-error) || true

              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              BODY=$(echo "$RESPONSE" | sed '$d')

              if [ "$HTTP_CODE" = "200" ]; then
                SUCCESS=true
                TOTAL_OK=$((TOTAL_OK + 1))
                echo "✅ HTTP $HTTP_CODE"
                echo "$BODY" | jq '.' || echo "$BODY"
              else
                RETRY_COUNT=$((RETRY_COUNT + 1))
                echo "⚠️  HTTP $HTTP_CODE (retry $RETRY_COUNT/$MAX_RETRIES)"
                echo "Response: $BODY"
                sleep $((RETRY_COUNT * 3))
              fi
            done

            if [ "$SUCCESS" = "false" ]; then
              TOTAL_FAIL=$((TOTAL_FAIL + 1))
              echo "❌ Call failed after retries"
            fi

            sleep $INTERVAL_SECONDS
          done

          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          echo "📈 Loop summary: OK=$TOTAL_OK FAIL=$TOTAL_FAIL"
          echo "⏱️  Total execution time: ${DURATION} seconds"
          echo "🎉 ULTRA mining batch completed!"


