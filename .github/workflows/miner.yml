name: Golden Keyword Miner Cron

on:
  schedule:
    - cron: '*/15 * * * *' # Run every 15 minutes (Write 최적화: 빈도 감소)
  workflow_dispatch: # Allow manual trigger

jobs:
  mine:
    runs-on: ubuntu-latest
    timeout-minutes: 10 # 3분 크론 내에서 루프 호출로 처리량 극대화 (8분 → 10분으로 증가)
    concurrency:
      group: keyword-expand-cron
      cancel-in-progress: false
    
    steps:
      - name: Trigger Miner API
        id: mining
        run: |
          set -e  # 에러 시 즉시 중단
          
          # 환경 변수 검증
          if [ -z "${{ secrets.PROD_URL }}" ] || [ -z "${{ secrets.CRON_SECRET }}" ]; then
            echo "❌ ERROR: PROD_URL or CRON_SECRET is not set in GitHub Secrets."
            exit 1
          fi
          
          START_TIME=$(date +%s)
          echo "🚀 Starting mining batch at $(date -u +"%Y-%m-%d %H:%M:%S UTC")"

          # 🚀 Write 최적화: 배치 크기 및 빈도 대폭 감소
          # - 배치 크기: 2000 → 300 (85% 감소)
          # - 동시성: 130 → 50 (62% 감소)
          # - 실행 시간: 7분 → 3분 (57% 감소)
          # - 호출 간격: 10초 → 30초 (3배 증가)
          # 예상 Write 감소: 약 90% 이상
          RUN_FOR_SECONDS=180  # 3분간 실행 (7분 → 3분, 57% 감소)
          INTERVAL_SECONDS=30  # 30초마다 호출 (10초 → 30초, 3배 증가)
          END_TIME=$((START_TIME + RUN_FOR_SECONDS))

          # 🚀 Write 최적화: 보수적 설정
          # - expandBatch=300 => 300 시드/실행 (2000 → 300, 85% 감소)
          # - expandConcurrency=50 => 50 동시성 (130 → 50, 62% 감소)
          # - minSearchVolume=100 => 최소 검색량 100 이상만 수집
          # 예상 Write: (180/30=6 calls/run) * 300 * 96 = 172,800 writes/day (이전 대비 90% 감소)
          QUERY="task=expand&mode=TURBO&expandBatch=300&expandConcurrency=50&minSearchVolume=100&maxRunMs=58000"

          TOTAL_OK=0
          TOTAL_FAIL=0

          while [ $(date +%s) -lt $END_TIME ]; do
            echo "📡 Calling miner: ${{ secrets.PROD_URL }}/api/miner/execute?$QUERY"

            # Per-call retry (keep short)
            MAX_RETRIES=2
            RETRY_COUNT=0
            SUCCESS=false

            while [ $RETRY_COUNT -lt $MAX_RETRIES ] && [ "$SUCCESS" = "false" ]; do
              RESPONSE=$(curl -X GET "${{ secrets.PROD_URL }}/api/miner/execute?$QUERY" \
                -H "CRON_SECRET: ${{ secrets.CRON_SECRET }}" \
                -w "\n%{http_code}" \
                --max-time 75 \
                --silent \
                --show-error) || true

              HTTP_CODE=$(echo "$RESPONSE" | tail -n1)
              BODY=$(echo "$RESPONSE" | sed '$d')

              if [ "$HTTP_CODE" = "200" ]; then
                SUCCESS=true
                TOTAL_OK=$((TOTAL_OK + 1))
                echo "✅ HTTP $HTTP_CODE"
                echo "$BODY" | jq '.' || echo "$BODY"
              else
                RETRY_COUNT=$((RETRY_COUNT + 1))
                echo "⚠️  HTTP $HTTP_CODE (retry $RETRY_COUNT/$MAX_RETRIES)"
                echo "Response: $BODY"
                sleep $((RETRY_COUNT * 3))
              fi
            done

            if [ "$SUCCESS" = "false" ]; then
              TOTAL_FAIL=$((TOTAL_FAIL + 1))
              echo "❌ Call failed after retries"
            fi

            sleep $INTERVAL_SECONDS
          done

          END_TIME=$(date +%s)
          DURATION=$((END_TIME - START_TIME))
          
          echo "📈 Loop summary: OK=$TOTAL_OK FAIL=$TOTAL_FAIL"
          echo "⏱️  Total execution time: ${DURATION} seconds"
          echo "🎉 Mining batch completed successfully!"

